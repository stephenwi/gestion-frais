{% extends 'base.html.twig' %}

{% block title %}Edit NoteFrais{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}
    <div class="container">
        <h1 class="text-center mt-5 mb-5">Editer  Note de Frais {{ note_frai.identifiant }}</h1>

        {{ include('note_frais/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="text-center">
             <a class="btn btn-info" href="{{ path('app_note_frais_index') }}">back to list</a>
        </div>
        {{ include('note_frais/_delete_form.html.twig') }}
    </div>
{% endblock %}
