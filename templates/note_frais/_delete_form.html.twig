<form method="post" action="{{ path('app_note_frais_delete', {'id': note_frai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_frai.id) }}">
    <div class="text-center mt-5">
        <button class="btn btn-danger">Delete</button>
    </div>
</form>
